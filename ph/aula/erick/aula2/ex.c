// Transforme o exemplo para ler n alunos do teclado (alocação dinâmica com malloc).

// Ordene por média decrescente com Bubble e, em caso de empate, por nome crescente (defina a regra no if usando chave secundária).

// Reescreva a ordenação por RM usando qsort e um comparador com chaves múltiplas: primeiro RM, em empate, nome (crescentemente).
